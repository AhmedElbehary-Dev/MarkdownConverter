# V2.0.0 Release Plan

- [x] Update version in `.csproj` files to `2.0.0`
- [x] Review `.github/workflows/dotnet-desktop.yml` to see how the release is triggered
- [x] Commit version bump
- [x] Create and push new tag `v2.0.0` (or create release via GitHub CLI)

## Review
- Version bumped in both Core and Desktop projects
- Changes committed and pushed to `main` branch
- New tag `v2.0.0` created and pushed to `origin`
- GitHub actions triggered, and a draft release can now be generated on GitHub UI using the tag.
